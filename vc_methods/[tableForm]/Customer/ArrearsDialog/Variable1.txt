//%attributes = {"lang":"en"} comment added and reserved by 4D.
If (bCalc=0)
AlertMessage (Get localized string("WARNING - Unchecking this option will cause report to show incorrect balances. Only uncheck this box if you are reprinting the report immediately after you have already printed it."))
End if 